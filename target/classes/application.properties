#CONFIG SOMENTE PARA BD MYSQL
spring.datasource.url=jdbc:mysql://localhost:3306/prodinfo
spring.datasource.username=root
spring.datasource.password=@12345Dev
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernates.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# ERRO CASO O USUÁRIO TENHA SIDO CONFIGURADO SEM SENHA
    # Em caso de erro de permissao para gravar dados no banco, executar as linhas 
    # abaixo na query do banco no MySQL Workbench
# CREATE USER 'nome_usuario'@'localhost' IDENTIFIED BY 'senha;
# GRANT ALL PRIVILEGES ON *.* TO 'nome_usuario'@'localhost' WITH GRANT OPTION;
